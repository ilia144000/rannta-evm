📘 RANNTA EVM — Polygon Expansion Layer
ERC-20 Mirror Contract + Bridge Controller (Phase 1–4)

RANNTA is a TON-native Jetton (ent_rannta_001) expanding into EVM networks as part of the upcoming multi-chain architecture of RANNTAverse.
This repository contains the first EVM deployment steps: a minimal ERC-20 mirror contract + a trust-based bridge controller used for infrastructure testing.

🚀 Overview

This project demonstrates:

✔️ Deployment of RANNTA ERC-20 Mirror on Polygon

✔️ Deployment of BridgeController (owner-restricted mint/burn)

✔️ Setting bridge authority inside the ERC-20 contract

✔️ Executing bridgeMint and bridgeBurn transactions

✔️ Verifying both contracts on PolygonScan

✔️ Establishing the foundation for future cross-chain bridging inside RANNTAverse

⚠️ Important:
This is not a real bridge and not bridged supply.
Total supply on Polygon is test-only and independent of RANNTA Jetton supply on TON.
No liquidity exists, no trading, and no public usage should occur until the real bridge is implemented.

📂 Repository Structure
rannta-evm/
│
├── contracts/
│   ├── RANNTA.sol                 # ERC-20 Mirror Token
│   └── BridgeController.sol       # Owner-based mint/burn bridge authority
│
├── scripts/
│   ├── deploy-rannta-polygon.ts   # Deployment script for ERC-20 Mirror
│   └── deploy-bridge-controller.ts# Deployment script for Bridge Controller
│
├── flattened/
│   └── RANNTA_flat.sol            # Auto-generated flattened file for verification
│
├── deployments/                   # Auto-created after running scripts
├── typechain-types/               # Generated by Hardhat
│
├── hardhat.config.ts              # Hardhat configuration
├── package.json
├── tsconfig.json
├── .env                           # Private keys / RPC (not committed)
└── README.md                      # This file

🧱 Contracts
1) RANNTA.sol

A minimal, gas-optimized ERC-20 mirror token:

18 decimals

No public mint or burn

Only bridge address can mint / burn

Initial supply = 0

Owner can update bridge once

2) BridgeController.sol

A simple controller enabling:

bridgeMint(address to, uint256 amount)

bridgeBurn(address from, uint256 amount)

setBridge(address newBridge) (called on the token contract)

All functions are onlyOwner.

🔧 Deployment Instructions (Polygon)
1. Install dependencies
npm install

2. Create .env
PRIVATE_KEY=0xYOUR_PRIVATE_KEY
POLYGON_RPC_URL=https://polygon.llamarpc.com

3. Compile contracts
npx hardhat compile

4. Deploy ERC-20 Mirror
npx hardhat run scripts/deploy-rannta-polygon.ts --network polygon


Outputs:
RANNTA deployed at: 0x899C6846F918d2Ff97b99366D7F8C2226BDE02d7

5. Deploy Bridge Controller
npx hardhat run scripts/deploy-bridge-controller.ts --network polygon


Outputs:
BridgeController deployed at: 0xA4c059c59B76B2258069A35886Ef6dB7FB0043bF

6. Set Bridge address on token

Using PolygonScan → RANNTA → Write Contract → setBridge()

newBridge = 0xA4c059c59B76B2258069A35886Ef6dB7FB0043bF

7. Test minting (infrastructure only)

On RANNTA → Write Contract → bridgeMint()

to = 0xYOUR_WALLET
amount = 10


Transaction should succeed and appear as:

Transfer 10 RANNTA to 0xYourWallet

🧪 Current Deployed Addresses (Polygon Mainnet)
Contract	Address
RANNTA (ERC-20 Mirror)	0x899C6846F918d2Ff97b99366D7F8C2226BDE02d7
BridgeController	0xA4c059c59B76B2258069A35886Ef6dB7FB0043bF

✔ Both verified on PolygonScan
✔ Both used for Phase-1 testing
✔ Functionality restricted (no public mint/burn)

🧩 Multi-Chain Roadmap (RANNTAverse)
Phase 1 — ERC-20 Mirror (Done)

Basic deployment on Polygon as architecture foundation.

Phase 2 — Bridge Authority (Done)

Owner-based mint/burn controller deployed.

Phase 3 — Verify + Integrate (Done)

PolygonScan verification + linking contracts.

Phase 4 — Manual Test Mint (Done)

Successful bridgeMint execution (10 units).

Phase 5 — Real Bridge Design (Upcoming)

Burn on TON → mint on Polygon

Mint on TON → burn on Polygon

Trustless actor or oracle

Message proofs

Security audit

⚠️ Security Notes (Important)

This is not a production bridge.

Never use real liquidity with this version.

The contracts are intentionally simple and centralized.

For public use, a fully trustless bridge must be implemented.

🤝 Credits

RANNTA / RANNTAverse
Symbolic Crypto-Art + TON Protocol Entity
ent_rannta_001

Developed and deployed by ilia144000
https://rannta.com

📜 License

MIT License (Open Source)
